# Dépendances Python pour infoconcert.lu
#
# scrape_atelier_concerts.py      → stdlib uniquement (pas de dépendance externe)
# scrape_rockhal_concerts.py      → stdlib uniquement (pas de dépendance externe)
# scrape_casino2000_concerts.py   → stdlib uniquement (pas de dépendance externe)
# scrape_kulturfabrik_concerts.py → stdlib uniquement (pas de dépendance externe)
# scrape_philharmonie_concerts.py → stdlib uniquement (pas de dépendance externe)
# scrape_echo_lu_concerts.py      → stdlib uniquement (pas de dépendance externe)
# ftp_upload.py                   → stdlib uniquement (pas de dépendance externe)
# merge.py                        → requiert requests

requests>=2.28.0
